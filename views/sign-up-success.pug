extends layout 

block content 
    div.top
        h1 VIP Club
        ul.top-links 
            li 
                a(href="/") Return to main page 
    p You have successfully signed up, #{user.username}! Proceed to #[a(href='/login') log in].